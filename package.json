{
  "name": "my-search-site",  // Use the actual name you chose if different
  "version": "1.0.0",
  "description": "Google Search Scraper running on Render (Experimental)", // Updated description
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",   // <--- הפסיק החשוב נמצא כאן
    "cheerio": "^1.0.0-rc.12"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
